# このリポジトリに置くのは、配る 1 ファイルと、その説明だけ。
#
# 原本には作りの理由が注釈として書いてあり、それは読まれたくないという判断で
# コメントを剥がした配布用を別に作っている。押し込んでしまえばその判断が無意味に
# なるので、`git add -A` のような一括追加で紛れ込まないよう、名指しで除外する。
#
# 手元では今までどおり全部そろっている。消えるわけではない。

osu-gd.source.js
osu-gd.comments.diff
build.py
verify.js
NOTES.md
